#!/bin/sh

# Pre-push hook to validate branch names
# Based on the original Husky pre-push hook

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"

cd "$PROJECT_ROOT" && docker compose -f dev-docker-compose.yml exec -T app npx validate-branch-name
